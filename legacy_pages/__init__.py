"""Legacy Streamlit pages kept for reference and selective reuse.

The production app routes only through app.py's three active menus. Files in
this package are intentionally outside Streamlit's pages/ directory so they do
not appear as standalone app pages.
"""
